> 用途：维护 Agent Runtime 学习主线、当前阶段目标与可验证产出。知识解释沉淀到 wiki/，个人复盘写入 posts/。

## 1. 方向与边界

目标定位：

    Agent Runtime / Agentic System Engineer

已有的 Android、客户端和设备侧经验是差异化资产与验证场景，不是职业边界。要建立的核心能力是：让 LLM、工具、状态、记忆与权限在长任务中稳定协作。

不把主线设为：模型训练、CUDA、纯 Prompt Engineering，或只做 Android 页面业务。

### 概念边界

| 层级 | 负责什么 | 不负责什么 |
| --- | --- | --- |
| LLM | 生成、推理、结构化输出 | 工具执行、长期状态、权限与审计 |
| Agent | 围绕目标调用 LLM 与工具的闭环 | 多任务调度、持久化治理 |
| Agent Runtime | Task、State、Context、Memory、Tool、Event 的运行与恢复 | 单一模型能力本身 |
| Agentic OS | Runtime、能力层、策略、调度与多 Agent 的系统愿景 | 当前阶段的直接实现目标 |

## 2. 学习原则

- 先理解 LLM 在系统中的边界，再学习 Tool Calling、Agent Loop、Context / Memory、Runtime 状态机。
- 每个主题都要有可验证产出：概念笔记、流程图、最小代码或评测任务。
- 前六个月只深入一个开发框架（OpenAI Agents SDK 或 LangGraph）和一个源码对象；其他只横向了解。
- 先做最小 Runtime，再逐步补齐权限、回放、验证和失败恢复；不堆叠框架。

## 3. 六个月路线

| 阶段 | 重点 | 可验证产出 |
| --- | --- | --- |
| 第 1 月 | LLM 基础、Structured Output、Tool Calling | 学习文稿；最小 LLM client；一个工具调用 demo |
| 第 2 月 | Agent Loop、Observation、Context / Memory | 可记录 event log 的最小 Agent Loop |
| 第 3 月 | Task Manager、Planner、Executor、Tool Registry、Verifier | 可恢复、可回放的 mini-agent-runtime v1 |
| 第 4 月 | 一个框架与一个开源 Runtime 的源码阅读 | 模块图、时序图、状态图与调用链说明 |
| 第 5 月 | Runtime 完整化 | 权限、重试、评测、任务报告与回归用例 |
| 第 6 月 | 垂直场景验证与作品化 | Coding Repo Review Agent 或 Android Log/Test Agent |

阶段验收只看三件事：能否解释设计取舍、能否实现最小闭环、能否用真实任务证明稳定性。

## 4. 当前阶段：Week1 LLM 基础认知

目标不是学完 Transformer，而是建立后续 Agent Runtime 必需的运行时直觉：文本如何进入模型、模型如何利用上下文、何时取外部信息，以及连续生成为什么昂贵。

### 需要达到的理解

| 主题                        | 应能解释                                                              |
| ------------------------- | ----------------------------------------------------------------- |
| Token / Embedding         | 文本如何变成 Token ID；Embedding 是可训练查表；初始向量不等于上下文语义                     |
| Attention / Transformer   | Q/K/V 如何聚合可见上下文；decoder-only 模型如何逐 Token 预测                       |
| Context Window / KV Cache | Context Window 是一次调用容量；prefill 与 decode 的区别；KV Cache 为什么加速、又为何占资源 |
| RAG                       | 离线建索引与在线检索的边界；检索向量不等于生成模型的 Token Embedding                        |
| Memory / State            | 为什么跨会话任务不能只靠 Prompt 或长上下文；Memory、RAG、State 分别解决什么问题               |

### Memory：本阶段的学习边界

本阶段达到 working 即可：能设计并评审一个最小 Memory 系统，不需要研究论文级实现或框架 API。

先分清：

| 概念 | 作用 |
| --- | --- |
| Context Window | 当前一次模型调用可见的工作区 |
| KV Cache | 推理加速用的历史 K/V 缓存，不是长期记忆 |
| RAG | 为当前问题取回外部证据的一种机制 |
| Memory | 跨任务、跨会话保存、检索、更新信息的系统能力 |
| State | 当前任务的进度、待办、权限和临时执行状态 |

需要掌握：

1. Memory 的对象至少包括用户偏好、历史决策/事件、任务状态；外部知识文档不应与用户记忆混为一谈。
2. 生命周期是：写入 → 带来源、时间、范围保存 → 检索与排序 → 拼入上下文 → 更新、过期或处理冲突。
3. 评估不只看“能否召回”，还要看误写入、陈旧与冲突、权限、可编辑/可删除，以及成本和延迟。

暂不深入：可微分记忆、Memory OS 的完整实现、复杂向量数据库选型、框架 API 和 benchmark 排名。

推荐阅读：

1. [Anthropic：Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)（优先）  
   关注有限上下文、按需检索、压缩与结构化笔记如何协作。
2. [MemGPT: Towards LLMs as Operating Systems](https://arxiv.org/abs/2310.08560)（选读）  
   阅读摘要、图 1、2.1～2.2 节；重点理解“主上下文 / 外部存储 / 按需调入”的分层模型，不把旧模型数据视为当前产品规格。

### 推荐顺序与产出

1. Token / Tokenizer：用 tiktoken 比较中文、英文和代码片段的 Token 数。  
   资源：[How to count tokens with tiktoken](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken)
2. Attention / Transformer：先读 [Google Transformer 介绍](https://research.google/blog/transformer-a-novel-neural-network-architecture-for-language-understanding/)，再回看《[Attention Is All You Need](https://arxiv.org/abs/1706.03762)》的图与 Attention 部分。
3. Context Window / KV Cache：阅读 [Hugging Face Caching](https://huggingface.co/docs/transformers/main/cache_explanation) 与《[Lost in the Middle](https://arxiv.org/abs/2307.03172)》。
4. RAG / Memory / State：阅读上述 Anthropic 文章；有余力再读 MemGPT。

本周产出：

- 一篇《从 Token 到 RAG》复盘文稿，包含运行时流程图与关键边界。
- 一个最小 LLM client：普通问答、structured JSON 输出、一个 calculator 工具，以及输入/输出/耗时/token 用量记录。

本周验收：

1. 为什么 Context Window 不等于长期 Memory？
2. prefill、decode 与 KV Cache 各自解决什么问题？
3. RAG 为什么不是完整的 Memory 系统？
4. 一个最小 Memory 系统应如何写入、检索、更新和删除信息？

## 5. 后续项目主线

1. **LLM Tool Calling Lab**：验证 Structured Output、工具 schema、错误处理与日志。
2. **Minimal Agent Loop**：实现“输入目标 → 组织上下文 → 调用模型 → 执行工具 → 观察结果 → 判断完成”。 
3. **Mini Agent Runtime**：加入 Task、Event Log、Context Manager、Memory Store、Verifier、Retry 与权限分级。
4. **垂直验证项目**：优先 Coding Repo Review Agent；Android Log/Test Agent 作为有差异化的备选场景。

## 6. 关键检查点

| 时间     | 应证明的能力                                 |
| ------ | -------------------------------------- |
| 第 1 月末 | 能解释模型、上下文与工具边界，并完成最小 Tool Calling demo |
| 第 3 月末 | 能实现有状态、可恢复、可回放的 Agent Runtime v1       |
| 第 6 月末 | 能用一个真实场景证明工具、记忆、验证与失败恢复的工程能力           |
