> 用途：维护 Agent Runtime 学习主线、当前阶段目标与可验证产出。知识解释沉淀到 wiki/，个人复盘写入 posts/。

## 1. 方向与边界

目标定位：

    Agent Runtime / Agentic System Engineer

已有的 Android、客户端和设备侧经验是差异化资产与验证场景，不是职业边界。要建立的核心能力是：让 LLM、工具、状态、记忆与权限在长任务中稳定协作。

不把主线设为：模型训练、CUDA、纯 Prompt Engineering，或只做 Android 页面业务。

### 概念边界

| 层级 | 负责什么 | 不负责什么 |
| --- | --- | --- |
| LLM | 生成、推理、结构化输出 | 工具执行、长期状态、权限与审计 |
| Agent | 围绕目标调用 LLM 与工具的闭环 | 多任务调度、持久化治理 |
| Agent Runtime | Task、State、Context、Memory、Tool、Event 的运行与恢复 | 单一模型能力本身 |
| Agentic OS | Runtime、能力层、策略、调度与多 Agent 的系统愿景 | 当前阶段的直接实现目标 |

## 2. 学习原则

- 先理解 LLM 在系统中的边界，再学习 Tool Calling、Agent Loop、Context / Memory、Runtime 状态机。
- 每个主题都要有可验证产出：概念笔记、流程图、最小代码或评测任务。
- 前六个月只深入一个开发框架（OpenAI Agents SDK 或 LangGraph）和一个源码对象；其他只横向了解。
- 先做最小 Runtime，再逐步补齐权限、回放、验证和失败恢复；不堆叠框架。

## 3. 六个月路线

| 阶段 | 重点 | 可验证产出 |
| --- | --- | --- |
| 第 1 月 | LLM 基础、Structured Output、Tool Calling | 学习文稿；最小 LLM client；一个工具调用 demo |
| 第 2 月 | Agent Loop、Observation、Context / Memory | 可记录 event log 的最小 Agent Loop |
| 第 3 月 | Task Manager、Planner、Executor、Tool Registry、Verifier | 可恢复、可回放的 mini-agent-runtime v1 |
| 第 4 月 | 一个框架与一个开源 Runtime 的源码阅读 | 模块图、时序图、状态图与调用链说明 |
| 第 5 月 | Runtime 完整化 | 权限、重试、评测、任务报告与回归用例 |
| 第 6 月 | 垂直场景验证与作品化 | Coding Repo Review Agent 或 Android Log/Test Agent |

阶段验收只看三件事：能否解释设计取舍、能否实现最小闭环、能否用真实任务证明稳定性。

## 4. 当前阶段：Week1 LLM 基础认知

Week1 的模型内部链路已经通过《[[2026-07-14-从Token到RAG-我这一周搭起的大模型基础认知地图]]》完成：Token、Embedding、Attention、Transformer、KV Cache 与 RAG 已形成整体认识。本阶段的剩余目标不是继续扩展 Transformer 细节，而是把这些概念落到一个可观察的最小 LLM 调用闭环中，为 Week2 的 Agent Loop 做准备。

### 本周目标与边界

目标：实现一个最小 LLM client，能够组织消息、请求结构化输出、调用一个 calculator 工具，并记录每一轮的 token、耗时、工具结果与错误。

本周达到 working 即可：能解释每个接口在运行时中的位置，能处理一条正常路径和几条典型失败路径。不深入模型训练、复杂向量数据库、多 Agent 调度、完整 Memory 框架或框架 API 的横向比较。

### 需要学习的知识

#### 1. Context Window：把上下文当作有限的运行时预算

- 理解一次请求中的指令、历史消息、工具定义、工具结果与用户问题会共同占用上下文；更长的窗口不保证模型能稳定利用每一段信息。
- 能为最小 client 设定输入与输出的 token 预算，并说明何时应截断、摘要或检索，而不是无差别累积历史。
- 阅读：
  1. [Anthropic：Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)（优先，关注上下文选择、压缩与按需检索）。
  2. [Lost in the Middle: How Language Models Use Long Contexts](https://arxiv.org/abs/2307.03172)（理解“能放入”不等于“能稳定使用”）。

#### 2. Inference：一次模型调用在运行时发生了什么

- 将已有的 prefill、decode 与 KV Cache 认识连接到 API 调用：组装输入、模型生成、可选流式返回、记录 usage；工具调用会插入新的模型轮次，而不是在模型内部直接执行代码。
- 能区分模型生成时间、工具执行时间与端到端时间，避免把它们混为一次“回答耗时”。
- 阅读：
  1. [Hugging Face：Caching](https://huggingface.co/docs/transformers/main/cache_explanation)（复习自回归生成与 KV Cache 的运行时含义）。
  2. [Hugging Face：Generation strategies](https://huggingface.co/docs/transformers/main/en/generation_strategies)（从生成接口回看推理过程）。

#### 3. Sampling：模型如何从下一个 Token 的分布中作出选择

- 理解 greedy、temperature、top-p、最大输出长度与停止条件的作用及取舍；它们控制生成行为，不改变模型参数。
- 为最小 client 选择一组可复现实验设置；对 JSON、工具参数等要求稳定的场景，优先减少不必要的随机性。
- 阅读：
  1. [Hugging Face：Generation strategies](https://huggingface.co/docs/transformers/main/en/generation_strategies)（重点阅读 greedy 与 sampling 小节）。

#### 4. Message：谁说的话、为何要回传工具结果

- 理解 System / Developer、User、Assistant 与 Tool Message 的职责；不同提供商的角色名称和优先级可能不同，应以实际 API 文档为准。
- 能解释完整输入不是“用户问题”本身，而是经组织后的消息序列；工具结果必须以可追溯的消息重新进入下一轮模型上下文。
- 阅读：
  1. [OpenAI：Conversation state](https://developers.openai.com/api/docs/guides/conversation-state)（理解多轮输入与对话状态的组织方式）。
  2. [Anthropic：Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)（关注长任务中消息历史为何需要筛选和压缩）。

#### 5. Structured Output：让模型输出成为可被程序消费的数据

- 理解 JSON Schema 约束的价值，以及“格式合法”不等于“内容真实”。
- 能定义一个小 Schema，处理拒答、字段缺失和业务校验失败；不要只依赖提示词要求“请返回 JSON”。
- 阅读：
  1. [OpenAI：Structured model outputs](https://developers.openai.com/api/docs/guides/structured-outputs)。
  2. [JSON Schema：Getting started](https://json-schema.org/learn/getting-started-step-by-step)（只需掌握 object、array、required、enum 等基础约束）。

#### 6. Function Calling / Tool Calling：模型提出请求，程序负责执行

- 走通 `工具定义（Schema）→ 模型请求调用 → 参数校验 → 本地执行 → 工具结果回传 → 模型给出最终回答` 的完整链路。
- 明确工具参数和工具结果都不天然可信：需要校验、错误处理、重试边界，以及对有副作用操作的权限控制。本周只实现无副作用的 calculator。
- 阅读：
  1. [OpenAI：Function calling](https://developers.openai.com/api/docs/guides/function-calling)。
  2. [Anthropic：Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)（关注工具输出如何造成上下文膨胀）。

#### 7. Hallucination：把“模型生成”与“已验证事实”分开

- 区分事实性错误、编造来源、无效工具参数与误读工具结果；Structured Output 只能约束格式，不能证明事实正确。
- 为最小 client 设计可执行边界：计算题以工具结果为准；证据不足时明确说明；日志中保留模型请求、工具结果与最终回答以便复查。
- 阅读：
  1. [OpenAI：Why language models hallucinate](https://openai.com/index/why-language-models-hallucinate/)。

#### 8. Latency and Cost：将运行时取舍变成可观测数据

- 每轮至少记录输入 token、输出 token、模型耗时、工具耗时、总耗时、调用轮数和失败/重试次数。
- 理解长输入主要影响 prefill 成本和首 token 延迟，长输出与工具循环会拉长端到端时间；实际价格和限额以所用服务商、模型版本为准。
- 阅读：
  1. [OpenAI：Latency optimization](https://developers.openai.com/api/docs/guides/latency-optimization)。
  2. [OpenAI：Cost optimization](https://developers.openai.com/api/docs/guides/cost-optimization)。

### 建议执行顺序

1. **第 1 天：Context Window、Inference、Sampling**。为一次普通问答打印输入/输出 token、端到端耗时，并用两组 sampling 设置比较结果。
2. **第 2 天：Message 与 Structured Output**。组织一段多轮消息，要求模型输出一个可验证的小 JSON 对象；记录解析、拒答与校验结果。
3. **第 3～4 天：Function / Tool Calling**。实现 calculator，完成一次工具调用和结果回传；补齐无效参数、工具异常、无需工具三种分支。
4. **第 5 天：Hallucination、Latency and Cost**。为三类测试用例检查最终回答是否以工具结果为依据，并汇总日志中的 token、耗时与失败原因。

### 本周产出与验收

本周产出：

- 更新后的 Week1 学习复盘（已有文章可按本节继续补充）。
- 一个最小 LLM client：普通问答、Structured Output、calculator Tool Calling，以及逐轮 event log。
- 三组可重复测试：无需工具、正常计算、工具参数或执行失败。

本周验收：

1. 为什么一次调用的 Context Window 不等于跨会话的 Memory？
2. Structured Output 与 Tool Calling 分别约束什么？为什么前者不能替代后者？
3. 工具结果为何必须回传给模型？它与模型直接“知道答案”有什么区别？
4. 如何从 event log 判断回答是否基于真实工具结果，以及哪一段造成主要延迟和成本？

## 5. 后续项目主线

1. **LLM Tool Calling Lab**：验证 Structured Output、工具 schema、错误处理与日志。
2. **Minimal Agent Loop**：实现“输入目标 → 组织上下文 → 调用模型 → 执行工具 → 观察结果 → 判断完成”。 
3. **Mini Agent Runtime**：加入 Task、Event Log、Context Manager、Memory Store、Verifier、Retry 与权限分级。
4. **垂直验证项目**：优先 Coding Repo Review Agent；Android Log/Test Agent 作为有差异化的备选场景。

## 6. 关键检查点

| 时间     | 应证明的能力                                 |
| ------ | -------------------------------------- |
| 第 1 月末 | 能解释模型、上下文与工具边界，并完成最小 Tool Calling demo |
| 第 3 月末 | 能实现有状态、可恢复、可回放的 Agent Runtime v1       |
| 第 6 月末 | 能用一个真实场景证明工具、记忆、验证与失败恢复的工程能力           |
